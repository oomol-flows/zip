version: 0.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install && pip install -r requirements.txt
icon: https://package-assets.oomol.com/packages/zip/0.0.1/files/package/icon.png
name: zip
displayName: ZIP
dependencies:
  {}
description: This project can package the files or folders in the selected path
  into a zip package
releaseNotes: add code repo
repository: https://github.com/oomol-flows/zip
